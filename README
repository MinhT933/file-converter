🧠 Golang Service - Server & Worker 

> Bootstrap: [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app) (App Router + TypeScript)

## ⚡ Quick Start

**Requirements**

* Node.js ≥ 18 (LTS recommended)
* Package manager: **pnpm** (khuyến nghị) / npm / yarn

```bash
# install deps
pnpm i

# run dev server
pnpm dev
# open http://localhost:3000
```

👉 Trang mặc định nằm ở `app/page.tsx`. Lưu file là trình duyệt tự reload.

---

## 🔑 Environment Variables

Tạo file **`.env.local`** (không commit) từ mẫu dưới đây:

```env
# Public (được gửi xuống client)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Private (chỉ dùng ở server)
API_SECRET=change-me
```

> Next.js tự động đọc `.env.local`. Không cần cấu hình thêm.

---

## 📂 Project Structure

```
.
├── app/                # App Router
│   ├── layout.tsx
│   ├── page.tsx
│   └── (routes ...)
├── components/         # UI components (Client/Server)
├── lib/                # helpers, fetchers
├── public/             # static assets
├── styles/             # global.css / tailwind.css (nếu dùng)
├── next.config.mjs
├── package.json
└── tsconfig.json
```

---

## 🧪 Scripts

Các lệnh phổ biến trong `package.json` (tuỳ project có thể khác):

```jsonc
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  }
}
```

* `pnpm dev` – chạy dev server ở `localhost:3000`
* `pnpm build` – build production (tạo `.next`)
* `pnpm start` – chạy build đã tạo (production)
* `pnpm lint` – ESLint
* `pnpm typecheck` – kiểm tra TypeScript

---

## 🎨 Fonts & Assets

Project dùng [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) để tự động tối ưu font **Geist** của Vercel.

```tsx
// app/layout.tsx
import { Geist, Geist_Mono } from "next/font/google" // hoặc from "geist/font"
```

> Có thể thay bằng font khác nếu muốn; `next/font` sẽ tự tạo CSS và preload hợp lý.

---

## 🐳 Docker (tùy chọn)

Chạy nhanh bằng Docker (production-like):

```yaml
# docker-compose.yml
services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
```

**Dockerfile** tối giản:

```dockerfile
FROM node:20-alpine AS deps
WORKDIR /app
COPY package.json pnpm-lock.yaml* yarn.lock* package-lock.json* ./
RUN corepack enable && pnpm i --frozen-lockfile || npm ci || yarn --frozen-lockfile

FROM node:20-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN pnpm build || npm run build || yarn build

FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/public ./public
COPY --from=builder /app/package.json ./package.json
EXPOSE 3000
CMD ["node", "node_modules/next/dist/bin/next", "start", "-p", "3000"]
```

Chạy:

```bash
docker compose up --build
```

---

## 🚀 Deploy

* **Vercel**: đơn giản nhất (import repo, set env, deploy).
* **Docker**: build image theo Dockerfile ở trên, push lên registry, chạy `docker run -p 3000:3000 <image>`.

> Lưu ý set biến môi trường production (`NODE_ENV=production`) & cung cấp `.env` tương ứng.

---

## ✅ Code Quality

* ESLint: cấu hình sẵn với Next.js
* Prettier: khuyến nghị bật trong editor
* TypeScript: chạy `pnpm typecheck` trước khi commit

**VS Code (gợi ý)**

```jsonc
// .vscode/settings.json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "typescript.tsdk": "node_modules/typescript/lib"
}
```

---

## 🛠️ Troubleshooting

* **Cổng 3000 bận**: đổi cổng `pnpm dev -- -p 3001`
* **Font không tải**: kiểm tra cấu hình `next/font` hoặc CSP
* **API lỗi CORS**: bật CORS ở backend hoặc dùng proxy (`NEXT_PUBLIC_API_URL`)
* **Build chậm**: xoá `.next`, `node_modules`, cài lại dep

```bash
rm -rf .next node_modules
pnpm i && pnpm build
```

---

## 📄 License

MIT (hoặc cập nhật theo license bạn dùng)

---

### Notes

* README này đã tinh gọn nội dung mặc định, bổ sung env, Docker, và guideline quality.
* Thay `NEXT_PUBLIC_API_URL` theo backend của bạn.
